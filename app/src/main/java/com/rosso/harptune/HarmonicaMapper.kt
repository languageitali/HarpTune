package com.rosso.harptune

object HarmonicaMapper {
    // Se eliminó frequencyToNote por falta de uso según el linter.
    // Si la requiere, añádala nuevamente con la anotación @Keep.

    fun mapNoteToHole(note: String): Int {
        return 0 // Lógica de mapeo
    }
}